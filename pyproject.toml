[project]
name = "wox-plugin-template-python"
version = "0.0.1"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["wox-plugin==0.0.73"]

[project.optional-dependencies]
dev = ["ruff", "mypy"]

[tool.ruff]
line-length = 140
